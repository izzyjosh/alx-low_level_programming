#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * _abs - function
 * @a: parameter
 * Return: 0 (success)
 */

int _abs(int a)
{
	if (n < 0)
	{
		return (n * -1);
	}
	else
	{
		return (n);
	}

}
